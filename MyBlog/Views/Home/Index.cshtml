@model IEnumerable<MyBlog.ViewModels.LoginVm>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<div class="table-responsive">
    <table class="table table-striped table-hover">
        <tr>
            <th>
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EmailLog)
            </th>
            <th>
            <th></th>
        </tr>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.ActionLink(" ", "Delete", new { id = item.EmailLog },new { @class = "glyphicon glyphicon-trash" })
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.EmailLog)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { id = item.EmailLog }) |
                        @Html.ActionLink("Details", "Details", new { id = item.EmailLog }) |
                        
                    </td>
                </tr>

            }
        </tbody>
    </table>
</div>